# MigraineAAT-KG UI environment
#
# The UI is statically exported and served by nginx in Docker. Point it to the
# FastAPI backend. Use an absolute URL when the UI and API are on different
# origins; a relative path works when both are served behind the same origin.

NEXT_PUBLIC_API_URL=http://localhost:8000
